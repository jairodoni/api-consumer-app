import React from 'react';
import { HeaderContainer, LogoTitle, Text } from './styles';

export function Header() {
  return (
    <HeaderContainer>
      <LogoTitle>API Consumer</LogoTitle>
      <Text>Welcome to app</Text>
    </HeaderContainer>
  );
}
